<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity"
    tools:theme="@style/AppTheme.Black">

    <!--
      ~ Reduced to app-owned views only: the shell (drawer, toolbar, bottom bar, FAB, sheet, the
      ~ refresh indicator and the @id/fragment container) now lives inside the ComposeView.
      ~ rootFrame and nightlyText survive because the app background (setAppBackground), the version
      ~ badge and the seasonal snowfall/eggfall overlays hang off them and keep working unchanged.
      -->
    <FrameLayout
        android:id="@+id/rootFrame"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.compose.ui.platform.ComposeView
            android:id="@+id/composeView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <TextView
            android:id="@+id/nightlyText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginHorizontal="48dp"
            android:layout_marginVertical="8dp"
            android:background="@drawable/bg_rounded_4dp"
            android:fontFamily="sans-serif-light"
            android:gravity="center"
            android:padding="4dp"
            android:textAllCaps="true"
            android:textSize="12sp"
            android:textStyle="bold"
            tools:backgroundTint="#a0ff0000"
            tools:text="Nightly\n20200503" />
    </FrameLayout>

</layout>
